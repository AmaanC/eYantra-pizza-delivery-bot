#include <stdio.h>
#include <stdlib.h>
#include <math.h>

#include "dijkstra.h"
#include "../map/map.h"

int main() {
    InitGraph();
    // Dijkstra(GetNodeByName("S"), GetNodeByName("H12"), 90, GetGraph());
    return 0;
}